Invoke the `fb-marketplace:generate-summary` skill to analyze product images and create FB Marketplace listing files.

**Target folder:** $ARGUMENTS

If no folder is specified, ask which folder in `listings/` to process.
